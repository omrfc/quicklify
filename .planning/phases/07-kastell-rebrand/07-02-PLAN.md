---
phase: 07-kastell-rebrand
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/commands/init.ts
  - src/commands/doctor.ts
  - src/commands/interactive.ts
  - src/commands/backup.ts
  - src/commands/restore.ts
  - src/commands/transfer.ts
  - src/commands/add.ts
  - src/commands/remove.ts
  - src/commands/config.ts
  - src/commands/destroy.ts
  - src/commands/domain.ts
  - src/commands/firewall.ts
  - src/commands/health.ts
  - src/commands/list.ts
  - src/commands/logs.ts
  - src/commands/maintain.ts
  - src/commands/monitor.ts
  - src/commands/secure.ts
  - src/commands/snapshot.ts
  - src/commands/ssh.ts
  - src/commands/status.ts
  - src/commands/update.ts
  - src/commands/restart.ts
  - src/core/deploy.ts
  - src/core/snapshot.ts
  - src/core/status.ts
  - src/core/backup.ts
  - src/core/secure.ts
  - src/core/firewall.ts
  - src/core/domain.ts
  - src/core/logs.ts
  - src/core/maintain.ts
  - src/core/tokens.ts
  - src/core/provision.ts
  - src/utils/cloudInit.ts
  - src/utils/sshKey.ts
  - src/utils/errorMapper.ts
  - src/utils/serverSelect.ts
  - src/utils/modeGuard.ts
  - src/utils/yamlConfig.ts
  - src/utils/configMerge.ts
  - src/providers/linode.ts
  - src/mcp/server.ts
  - src/mcp/index.ts
  - src/mcp/tools/serverInfo.ts
  - src/mcp/tools/serverBackup.ts
  - src/mcp/tools/serverManage.ts
  - src/mcp/tools/serverProvision.ts
  - src/mcp/tools/serverSecure.ts
  - src/mcp/tools/serverMaintain.ts
  - src/mcp/tools/serverLogs.ts
  - src/index.ts
autonomous: true
requirements:
  - BRAND-01
  - BRAND-03
  - BRAND-04
  - BRAND-08

must_haves:
  truths:
    - "grep -ri 'quicklify' src/ returns zero hits (only src/utils/migration.ts has OLD_CONFIG_DIR which references .quicklify)"
    - "grep -ri 'quicklify' tests/ returns zero hits (except backward compat test assertions for QUICKLIFY_SAFE_MODE)"
    - "CLI program name is 'kastell' (program.name('kastell'))"
    - "MCP tool descriptions say 'Kastell' not 'Quicklify'"
    - "process.title is set to 'kastell' in init command"
    - "Snapshot prefix is 'kastell-' for new snapshots"
    - "Linode snapshot filter accepts both 'kastell-' and 'quicklify-' prefixes"
    - "Cloud-init log file is kastell-install.log"
    - "SSH key comment/name uses 'kastell' prefix"
    - "Default export filename is kastell-export.json"
  artifacts:
    - path: "src/index.ts"
      provides: "CLI entry with kastell name"
      contains: ".name(\"kastell\")"
    - path: "src/mcp/server.ts"
      provides: "MCP server with kastell branding in all 7 tool descriptions"
      contains: "Kastell"
    - path: "src/providers/linode.ts"
      provides: "Dual-prefix snapshot filter"
      contains: "kastell-"
  key_links:
    - from: "src/commands/init.ts"
      to: "process.title"
      via: "process.title = 'kastell'"
      pattern: "process\\.title.*kastell"
    - from: "src/providers/linode.ts"
      to: "snapshot filter"
      via: "dual-prefix startsWith check"
      pattern: "startsWith.*kastell.*startsWith.*quicklify"
    - from: "src/mcp/server.ts"
      to: "tool descriptions"
      via: "7 tool registrations with Kastell branding"
      pattern: "Kastell"
---

<objective>
Rename all remaining "quicklify" references across source files (commands, core, utils, providers, MCP) and all test files to "kastell".

Purpose: Complete the string replacement so that zero "quicklify" references remain in src/ and tests/ (except migration.ts OLD_CONFIG_DIR and backward compat test assertions).
Output: All ~42 src files and ~32 test files updated with kastell branding.
</objective>

<execution_context>
@C:/Users/Omrfc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Omrfc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-kastell-rebrand/07-CONTEXT.md
@.planning/phases/07-kastell-rebrand/07-RESEARCH.md
@.planning/phases/07-kastell-rebrand/07-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these names exist: -->
<!-- Types: KastellYamlConfig, KastellConfig, KastellResult<T> -->
<!-- Config: CONFIG_DIR = join(homedir(), ".kastell") in config.ts, defaults.ts, updateCheck.ts -->
<!-- Migration: migrateConfigIfNeeded() in src/utils/migration.ts -->
<!-- Env var: isSafeMode() checks KASTELL_SAFE_MODE first, then QUICKLIFY_SAFE_MODE -->

From src/core/deploy.ts (19 references):
  - All user-facing messages contain "quicklify status", "quicklify firewall", etc.
  - These become "kastell status", "kastell firewall", etc.

From src/mcp/server.ts (7 tool descriptions):
  - "Quicklify-managed servers" -> "Kastell-managed servers"
  - "QUICKLIFY_SAFE_MODE" in descriptions -> "KASTELL_SAFE_MODE"
  - Tool descriptions reference "Quicklify" in descriptions

From src/commands/doctor.ts:
  - checkQuicklifyVersion function -> checkKastellVersion
  - "Quicklify Doctor" output heading

From src/utils/cloudInit.ts:
  - "quicklify-install.log" -> "kastell-install.log"
  - "Quicklify Auto-Installer" -> "Kastell Auto-Installer"

From src/utils/sshKey.ts:
  - SSH key name prefix "quicklify" -> "kastell"
  - SSH key comment contains "quicklify"

From src/commands/transfer.ts:
  - Default export filename "quicklify-export.json" -> "kastell-export.json"

From src/providers/linode.ts:
  - Snapshot filter: img.label.startsWith("quicklify-")
  - Must become dual-prefix: startsWith("kastell-") || startsWith("quicklify-")

From src/core/snapshot.ts:
  - Snapshot name prefix "quicklify-" -> "kastell-"

From src/commands/init.ts:
  - process.title = "quicklify" -> process.title = "kastell"

From src/index.ts:
  - .name("quicklify") -> .name("kastell")
  - .description() may reference quicklify
  - --config option help text may mention quicklify.yml -> kastell.yml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all quicklify references in src/ files</name>
  <files>
    src/index.ts,
    src/commands/*.ts (all 23 command files),
    src/core/*.ts (all core files),
    src/utils/cloudInit.ts,
    src/utils/sshKey.ts,
    src/utils/errorMapper.ts,
    src/utils/serverSelect.ts,
    src/utils/modeGuard.ts,
    src/utils/yamlConfig.ts,
    src/utils/configMerge.ts,
    src/providers/linode.ts,
    src/mcp/server.ts,
    src/mcp/index.ts,
    src/mcp/tools/*.ts (all 7 tool files)
  </files>
  <action>
    Systematically rename all remaining "quicklify" references in src/ files. Work category by category:

    **CRITICAL -- Context-aware replacement rules (NOT blind find-and-replace):**
    - PascalCase "Quicklify" in prose/descriptions -> "Kastell" (e.g., "Quicklify-managed" -> "Kastell-managed")
    - lowercase "quicklify" in CLI command refs -> "kastell" (e.g., "quicklify init" -> "kastell init")
    - lowercase ".quicklify" in config paths -> ".kastell" (already done in Plan 01 for config/defaults/updateCheck)
    - "quicklify-install.log" -> "kastell-install.log" (cloud-init log path)
    - "quicklify-" snapshot prefix -> "kastell-" for new snapshots
    - "quicklify-export.json" -> "kastell-export.json" (transfer default filename)
    - "QUICKLIFY_SAFE_MODE" in MCP tool descriptions -> "KASTELL_SAFE_MODE" (but note: the actual env var check was already handled in Plan 01, these are just string literals in description text)
    - Function name: checkQuicklifyVersion -> checkKastellVersion
    - SSH key: "quicklify" key name/comment -> "kastell"
    - process.title = "quicklify" -> "kastell"
    - quicklify.yml reference in --config help -> kastell.yml

    **DO NOT touch:**
    - src/utils/migration.ts (intentionally references ".quicklify" as OLD_CONFIG_DIR)
    - CHANGELOG entries (handled in Plan 03)

    **Category 1: CLI entry point (src/index.ts)**
    - .name("quicklify") -> .name("kastell")
    - Update --config help text if it mentions quicklify.yml -> kastell.yml
    - Update program description if it mentions "Quicklify"

    **Category 2: High-risk files (complex changes)**
    - src/core/deploy.ts: 19 references -- all "quicklify <command>" -> "kastell <command>" in user-facing messages
    - src/mcp/server.ts: 7 tool descriptions -- "Quicklify-managed" -> "Kastell-managed", "QUICKLIFY_SAFE_MODE" -> "KASTELL_SAFE_MODE" in description strings
    - src/mcp/tools/serverInfo.ts: 9 references -- CLI command suggestions
    - src/utils/cloudInit.ts: 8 references -- log file names, banners
    - src/commands/doctor.ts: 6 references -- function name + output strings

    **Category 3: MCP tools (4 references each)**
    - src/mcp/tools/serverBackup.ts: SAFE_MODE references in descriptions
    - src/mcp/tools/serverManage.ts: SAFE_MODE + command references
    - src/mcp/tools/serverProvision.ts, serverSecure.ts, serverMaintain.ts, serverLogs.ts: check each for quicklify references

    **Category 4: Providers**
    - src/providers/linode.ts: Change snapshot filter to dual-prefix: `img.label.startsWith("kastell-") || img.label.startsWith("quicklify-")` -- this ensures existing quicklify-* snapshots remain visible

    **Category 5: Core + Utils**
    - src/core/snapshot.ts: snapshot name prefix "quicklify-" -> "kastell-"
    - src/utils/sshKey.ts: SSH key name/comment "quicklify" -> "kastell"
    - src/utils/errorMapper.ts: any CLI command references
    - src/utils/serverSelect.ts: "quicklify init" message
    - src/utils/modeGuard.ts: any quicklify references
    - src/commands/transfer.ts: default export filename "quicklify-export.json" -> "kastell-export.json"

    **Category 6: All 23 command files**
    - src/commands/init.ts: process.title = "kastell", all user messages
    - Remaining 22 command files: user-facing messages with "quicklify" -> "kastell"

    **Verification after each category:** Run `npm run build` to catch TypeScript errors immediately.

    **Final verification:** `grep -ri "quicklify" src/ | grep -v migration.ts` should return 0 hits.
  </action>
  <verify>
    <automated>npm run build && bash -c 'HITS=$(grep -ri "quicklify" src/ | grep -v migration.ts | wc -l); echo "Remaining quicklify hits in src/: $HITS"; [ "$HITS" -eq 0 ]'</automated>
  </verify>
  <done>
    - Zero "quicklify" references in src/ (except src/utils/migration.ts OLD_CONFIG_DIR)
    - CLI program name is "kastell"
    - All MCP tool descriptions reference "Kastell" and "KASTELL_SAFE_MODE"
    - Linode snapshot filter accepts both "kastell-" and "quicklify-" prefixes
    - Cloud-init uses "kastell-install.log"
    - SSH key uses "kastell" prefix
    - Default export filename is "kastell-export.json"
    - `npm run build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all test files to match source renames</name>
  <files>
    tests/unit/*.test.ts (all ~32 test files with quicklify references),
    tests/integration/*.test.ts,
    tests/e2e/*.test.ts
  </files>
  <action>
    Update all ~32 test files with ~140 quicklify references. Categories:

    **Mock values:**
    - `getSshKeyName: jest.fn().mockReturnValue("quicklify-test")` -> `"kastell-test"`
    - Snapshot name mocks: "quicklify-*" -> "kastell-*"

    **Path assertions:**
    - `expect(CONFIG_DIR).toContain(".quicklify")` -> `.kastell`
    - Any mock path containing ".quicklify" -> ".kastell"

    **Env var references:**
    - `process.env.QUICKLIFY_SAFE_MODE` -> `process.env.KASTELL_SAFE_MODE` for primary test paths
    - KEEP backward compat test cases that explicitly test QUICKLIFY_SAFE_MODE still working (these intentionally use the old name)

    **Output assertions:**
    - `expect(output).toContain("quicklify init")` -> `"kastell init"`
    - All user-facing message assertions updated

    **Type imports:**
    - `QuicklifyYamlConfig` -> `KastellYamlConfig`
    - `QuicklifyConfig` -> `KastellConfig`
    - `QuicklifyResult` -> `KastellResult`

    **Function name references:**
    - `checkQuicklifyVersion` -> `checkKastellVersion`

    **Allowed exceptions in tests:**
    - Tests that explicitly verify QUICKLIFY_SAFE_MODE backward compat (these should reference the old env var name intentionally)
    - Tests for migration.ts that reference ".quicklify" as the old path (these should reference old name intentionally)
    - Tests for Linode dual-prefix filter that check both "quicklify-" and "kastell-" prefixes

    **Final verification:** `grep -ri "quicklify" tests/ | grep -v "QUICKLIFY_SAFE_MODE" | grep -v "\.quicklify" | grep -v "quicklify-"` -- should return 0 hits (the excluded patterns are intentional backward compat references).

    Run the full test suite after all changes.
  </action>
  <verify>
    <automated>npm run build && npm test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All ~2099 tests pass
    - Zero unintentional "quicklify" references in test files
    - Only intentional backward compat test assertions retain "quicklify" references (env var compat, migration source path, Linode dual-prefix)
    - `npm run build && npm test` both succeed
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm test` passes all tests
- `npm run lint` passes
- `grep -ri "quicklify" src/ | grep -v migration.ts` returns 0 hits
- `grep -ri "quicklify" tests/ | grep -v QUICKLIFY_SAFE_MODE | grep -v "\.quicklify" | grep -v "quicklify-"` returns 0 hits (only intentional backward compat refs remain)
</verification>

<success_criteria>
- Zero unintentional "quicklify" references remain in src/ and tests/
- CLI program name is "kastell"
- MCP tool descriptions use "Kastell" branding
- Linode snapshot filter accepts both prefixes for backward compat
- All ~2099 tests pass with zero regressions
- Build + lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-kastell-rebrand/07-02-SUMMARY.md`
</output>
