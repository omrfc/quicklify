---
phase: 07-kastell-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/utils/config.ts
  - src/utils/defaults.ts
  - src/utils/updateCheck.ts
  - src/utils/migration.ts
  - src/core/manage.ts
  - src/index.ts
  - src/mcp/index.ts
  - tests/unit/migration.test.ts
  - tests/unit/manage-safemode.test.ts
autonomous: true
requirements:
  - BRAND-02
  - BRAND-09
  - BRAND-03

must_haves:
  truths:
    - "Config is read from ~/.kastell/ directory, not ~/.quicklify/"
    - "If ~/.quicklify exists and ~/.kastell does not, contents are copied automatically on first run"
    - "KASTELL_SAFE_MODE=true enables safe mode"
    - "QUICKLIFY_SAFE_MODE=true still works with a deprecation warning"
    - "Internal types are named KastellYamlConfig, KastellConfig, KastellResult"
    - "Update check queries registry.npmjs.org/kastell/latest"
  artifacts:
    - path: "src/utils/migration.ts"
      provides: "Config directory migration logic"
      exports: ["migrateConfigIfNeeded"]
    - path: "src/types/index.ts"
      provides: "Renamed type definitions"
      contains: "KastellResult"
    - path: "src/utils/config.ts"
      provides: "Config path pointing to ~/.kastell"
      contains: ".kastell"
    - path: "tests/unit/migration.test.ts"
      provides: "Migration logic tests"
      min_lines: 50
  key_links:
    - from: "src/index.ts"
      to: "src/utils/migration.ts"
      via: "migrateConfigIfNeeded() call before config access"
      pattern: "migrateConfigIfNeeded"
    - from: "src/mcp/index.ts"
      to: "src/utils/migration.ts"
      via: "migrateConfigIfNeeded() call before MCP server starts"
      pattern: "migrateConfigIfNeeded"
    - from: "src/core/manage.ts"
      to: "process.env.KASTELL_SAFE_MODE"
      via: "isSafeMode() dual-check logic"
      pattern: "KASTELL_SAFE_MODE"
---

<objective>
Rename internal foundation: types, config paths, env vars, migration logic, and update check -- the plumbing that all other rebrand work depends on.

Purpose: Establish the internal identity as "kastell" so subsequent plans can rename user-facing strings against a consistent foundation.
Output: Renamed types, ~/.kastell config path, migration function, dual env var support, updated update-check URL.
</objective>

<execution_context>
@C:/Users/Omrfc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Omrfc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-kastell-rebrand/07-CONTEXT.md
@.planning/phases/07-kastell-rebrand/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/index.ts (lines 81-151):
```typescript
export interface QuicklifyYamlConfig {
  template?: TemplateName;
  provider?: string;
  region?: string;
  size?: string;
  name?: string;
  fullSetup?: boolean;
  domain?: string;
}

export interface QuicklifyConfig {
  provider?: string;
  region?: string;
  size?: string;
  name?: string;
}

export interface QuicklifyResult<T = void> {
  success: boolean;
  data?: T;
  error?: string;
  hint?: string;
}
```

From src/utils/config.ts (line 6-8):
```typescript
const CONFIG_DIR = join(homedir(), ".quicklify");
const SERVERS_FILE = join(CONFIG_DIR, "servers.json");
const BACKUPS_DIR = join(CONFIG_DIR, "backups");
```

From src/utils/defaults.ts (line 7-8):
```typescript
const CONFIG_DIR = join(homedir(), ".quicklify");
const DEFAULTS_FILE = join(CONFIG_DIR, "config.json");
```

From src/utils/updateCheck.ts (line 7-8, 79, 105, 123):
```typescript
const CONFIG_DIR = join(homedir(), ".quicklify");
export const UPDATE_CHECK_FILE = join(CONFIG_DIR, ".update-check");
// Line 79: axios.get("https://registry.npmjs.org/quicklify/latest")
// Lines 105, 123: "Run: npm i -g quicklify"
```

From src/core/manage.ts (line 11-13):
```typescript
export function isSafeMode(): boolean {
  return process.env.QUICKLIFY_SAFE_MODE === "true";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create migration utility and rename types + config paths</name>
  <files>
    src/utils/migration.ts,
    src/types/index.ts,
    src/utils/config.ts,
    src/utils/defaults.ts,
    src/utils/updateCheck.ts,
    src/core/manage.ts,
    src/index.ts,
    src/mcp/index.ts,
    tests/unit/migration.test.ts
  </files>
  <behavior>
    - migrateConfigIfNeeded: when ~/.quicklify exists and ~/.kastell does not, copies directory contents to ~/.kastell and creates .migrated flag
    - migrateConfigIfNeeded: when ~/.kastell already exists, does nothing (no overwrite)
    - migrateConfigIfNeeded: when ~/.quicklify does not exist, does nothing (fresh install)
    - migrateConfigIfNeeded: shows chalk.yellow warning message after successful migration
    - isSafeMode: returns true when KASTELL_SAFE_MODE=true
    - isSafeMode: returns true when only QUICKLIFY_SAFE_MODE=true (with deprecation warning to stderr)
    - isSafeMode: KASTELL_SAFE_MODE takes precedence when both are set (no warning)
    - isSafeMode: deprecation warning prints only once per process (module-level flag)
    - isSafeMode: returns false when neither env var is set
  </behavior>
  <action>
    **Step 1: Create src/utils/migration.ts** (new file)
    - Import: existsSync, mkdirSync, cpSync, writeFileSync from "fs"; homedir from "os"; join from "path"; chalk
    - Define OLD_CONFIG_DIR = join(homedir(), ".quicklify")
    - Define NEW_CONFIG_DIR = join(homedir(), ".kastell")
    - Define MIGRATED_FLAG = join(NEW_CONFIG_DIR, ".migrated")
    - Export function migrateConfigIfNeeded(): void
      - If NEW_CONFIG_DIR exists -> return immediately (no overwrite risk)
      - If OLD_CONFIG_DIR does not exist -> return (fresh install)
      - mkdirSync(NEW_CONFIG_DIR, { recursive: true, mode: 0o700 })
      - cpSync(OLD_CONFIG_DIR, NEW_CONFIG_DIR, { recursive: true })
      - writeFileSync(MIGRATED_FLAG, new Date().toISOString(), { mode: 0o600 })
      - console.warn(chalk.yellow("Migrated config from ~/.quicklify to ~/.kastell. You can safely remove ~/.quicklify."))
    - Wrap the copy+flag+warn in try-catch for robustness -- if copy fails, log warning and continue

    **Step 2: Rename types in src/types/index.ts**
    - QuicklifyYamlConfig -> KastellYamlConfig
    - QuicklifyConfig -> KastellConfig
    - QuicklifyResult<T> -> KastellResult<T>

    **Step 3: Update src/utils/config.ts**
    - Change CONFIG_DIR from ".quicklify" to ".kastell"

    **Step 4: Update src/utils/defaults.ts**
    - Change CONFIG_DIR from ".quicklify" to ".kastell"
    - Change import QuicklifyConfig -> KastellConfig

    **Step 5: Update src/utils/updateCheck.ts**
    - Change CONFIG_DIR from ".quicklify" to ".kastell"
    - Change npm registry URL from "quicklify/latest" to "kastell/latest"
    - Change install message from "npm i -g quicklify" to "npm i -g kastell"

    **Step 6: Update src/core/manage.ts isSafeMode()**
    - Add module-level `let _safeModeWarningShown = false;`
    - Rewrite isSafeMode():
      1. Read KASTELL_SAFE_MODE -- if defined, return kastell === "true" (no warning)
      2. Read QUICKLIFY_SAFE_MODE -- if defined and _safeModeWarningShown is false, set flag to true, write deprecation warning to stderr via process.stderr.write(chalk.yellow(...))
      3. Return quicklify === "true"
      4. If neither set, return false
    - Import chalk (add to existing imports)

    **Step 7: Wire migration into entry points**
    - In src/index.ts: import { migrateConfigIfNeeded } from "./utils/migration.js" and call it early (before Commander parse, after imports). Place it right after the `const pkg = ...` block.
    - In src/mcp/index.ts: import and call migrateConfigIfNeeded() before createMcpServer()

    **Step 8: Update all files that import renamed types**
    - grep for QuicklifyYamlConfig, QuicklifyConfig, QuicklifyResult across src/ and update each import to the new name (KastellYamlConfig, KastellConfig, KastellResult). This includes:
      - src/utils/yamlConfig.ts (QuicklifyYamlConfig)
      - src/utils/configMerge.ts (QuicklifyYamlConfig)
      - src/utils/defaults.ts (QuicklifyConfig)
      - All core/ files returning QuicklifyResult (deploy.ts, manage.ts, status.ts, tokens.ts, secure.ts, firewall.ts, domain.ts, backup.ts, snapshot.ts, maintain.ts, logs.ts)
      - src/mcp/utils.ts (if imports QuicklifyResult)

    **Step 9: Write tests/unit/migration.test.ts**
    - Mock fs (existsSync, mkdirSync, cpSync, writeFileSync) and os (homedir)
    - Test cases per behavior block above
    - Also add/update tests for isSafeMode() dual-check behavior in tests/unit/manage-safemode.test.ts (or the existing test file that covers isSafeMode)

    Run `npm run build` after all changes to verify TypeScript compilation succeeds.
  </action>
  <verify>
    <automated>npm run build && npx jest tests/unit/migration.test.ts tests/unit/manage-safemode -x --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/utils/migration.ts exists with migrateConfigIfNeeded() exported
    - All three CONFIG_DIR definitions point to ".kastell"
    - QuicklifyYamlConfig/QuicklifyConfig/QuicklifyResult renamed to Kastell* everywhere in src/
    - isSafeMode() checks KASTELL_SAFE_MODE first, falls back to QUICKLIFY_SAFE_MODE with one-time deprecation warning
    - updateCheck.ts queries kastell registry and shows "npm i -g kastell"
    - Migration function wired into src/index.ts and src/mcp/index.ts
    - All migration tests and safe mode tests pass
    - `npm run build` succeeds with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update existing tests for foundation changes</name>
  <files>
    tests/unit/config.test.ts,
    tests/unit/defaults.test.ts,
    tests/unit/update-check.test.ts,
    tests/unit/restore-safemode.test.ts,
    tests/unit/mcp-server-manage.test.ts
  </files>
  <action>
    Update existing test files that reference the old names/paths to match foundation changes:

    **Config tests (tests/unit/config.test.ts):**
    - Change all ".quicklify" path expectations to ".kastell"
    - Update any mock paths referencing quicklify config dir

    **Defaults tests (tests/unit/defaults.test.ts):**
    - Change QuicklifyConfig type references to KastellConfig
    - Change ".quicklify" path expectations to ".kastell"

    **Update check tests (tests/unit/update-check.test.ts):**
    - Change npm registry URL expectations from "quicklify" to "kastell"
    - Change "npm i -g quicklify" assertion to "npm i -g kastell"
    - Change ".quicklify" path expectations to ".kastell"

    **Safe mode tests (tests/unit/restore-safemode.test.ts and tests/unit/mcp-server-manage.test.ts):**
    - Update env var references from QUICKLIFY_SAFE_MODE to KASTELL_SAFE_MODE where testing the primary path
    - Add test cases for backward compat: setting only QUICKLIFY_SAFE_MODE should still enable safe mode
    - Add test case: both set, KASTELL_SAFE_MODE takes precedence

    **For ALL test files touched:** grep for any remaining "quicklify" references and update them. Also update any QuicklifyResult/QuicklifyConfig/QuicklifyYamlConfig type imports.

    Run the full test suite after all changes.
  </action>
  <verify>
    <automated>npm run build && npm test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All ~2099 existing tests pass with renamed types and config paths
    - Zero "quicklify" references remain in updated test files (except QUICKLIFY_SAFE_MODE backward compat test assertions)
    - `npm run build && npm test` both succeed
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (TypeScript compiles with all renames)
- `npm test` passes all ~2099 tests
- `grep -ri "QuicklifyYamlConfig\|QuicklifyConfig\|QuicklifyResult" src/` returns 0 hits
- `grep -r "\.quicklify" src/utils/config.ts src/utils/defaults.ts src/utils/updateCheck.ts` returns 0 hits (only migration.ts retains OLD_CONFIG_DIR reference)
</verification>

<success_criteria>
- Internal type identity is "Kastell" (KastellConfig, KastellResult, KastellYamlConfig)
- Config directory is ~/.kastell with automatic migration from ~/.quicklify
- KASTELL_SAFE_MODE is the primary env var, QUICKLIFY_SAFE_MODE works with deprecation warning
- Update check targets the kastell npm package
- All existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-kastell-rebrand/07-01-SUMMARY.md`
</output>
