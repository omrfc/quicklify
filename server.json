{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.kastelldev/kastell",
  "description": "Server security audit (448 checks), hardening, and fleet management across 4 cloud providers.",
  "repository": {
    "url": "https://github.com/kastelldev/kastell",
    "source": "github"
  },
  "version": "1.14.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "kastell",
      "version": "1.14.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "HETZNER_TOKEN",
          "description": "Hetzner Cloud API token (optional — only for Hetzner servers)",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "DIGITALOCEAN_TOKEN",
          "description": "DigitalOcean API token (optional — only for DigitalOcean servers)",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "VULTR_TOKEN",
          "description": "Vultr API token (optional — only for Vultr servers)",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "LINODE_TOKEN",
          "description": "Linode API token (optional — only for Linode servers)",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        }
      ]
    }
  ]
}
